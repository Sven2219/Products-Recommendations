import { Dimensions } from "react-native";

const { width, height } = Dimensions.get("window");


export const PRODUCT_CONTAINER_WIDTH = width * 0.9;
export const PRODUCT_CONTAINER_HEIGHT = height * 0.2;
export const DELIVERY_TRUCK_SIZE = 35;
export const PRODUCT_IMAGE_WIDTH = 100;